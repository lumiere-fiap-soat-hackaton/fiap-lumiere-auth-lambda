{
  "name": "@lumiere/sign-in",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "tsc -p tsconfig.json",
    "postbuild": "cp package.json ./dist/package.json"
  },
  "dependencies": {
    "@lumiere/shared": "^1.0.0",
    "bs-logger": "^0.2.6"
  }
}
