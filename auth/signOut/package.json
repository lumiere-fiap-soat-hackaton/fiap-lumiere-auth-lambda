{
  "name": "@lumiere/sign-out",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx jest --passWithNoTests",
    "prebuild": "rm -rf ../../build/sign-out/dist",
    "build": "tsc -p tsconfig.json",
    "postbuild": "cp package.json ../../build/sign-out/dist/",
    "prepackage": "npm install --omit=dev && cp -rL ../../node_modules/. ../../build/sign-out/dist/node_modules",
    "package": "cd ../../build/sign-out/dist && zip -q -r ../../../../../infra/artifacts/signOut.zip .",
    "postpackage": "rm -rf ../../node_modules"
  },
  "dependencies": {
    "@lumiere/shared": "^1.0.0",
    "aws-lambda": "^1.0.7",
    "bs-logger": "^0.2.6"
  }
}
