{
  "name": "@lumiere/user-data",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx jest --passWithNoTests",
    "prebuild": "rm -rf ../../build/user-data/dist",
    "build": "tsc -p tsconfig.json",
    "postbuild": "cp package.json ../../build/user-data/dist/",
    "prepackage": "npm install --omit=dev && cp -rL ../../node_modules/. ../../build/user-data/dist/node_modules",
    "package": "cd ../../build/user-data/dist && zip -q -r ../../../../../infra/artifacts/userData.zip .",
    "postpackage": "rm -rf ../../node_modules"
  },
  "dependencies": {
    "@lumiere/shared": "^1.0.0",
    "aws-lambda": "^1.0.7",
    "bs-logger": "^0.2.6"
  }
}